apiVersion: 1

# Auto-provision the Prometheus datasource so the dashboard works on first boot.
datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
